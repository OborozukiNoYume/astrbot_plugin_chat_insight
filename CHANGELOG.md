# 更新日志

## v0.5.0 (2026-08-31)

- **破坏性变更**：移除 `/聊天统计` 命令组（总览/趋势/时段/关键词/关键词趋势）与 `/昵称` 命令，命令入口 12→6。依据全期调用统计：十四次统计子命令调用全部来自开发者测试期，群友零调用
- 替代路径：时间窗分析看 `/群画像`（卡片含 24h 分布与日趋势）；关键词看 `/词云`（无文本时自动降级文字词频）
- 同步清理 service/repository/render/cardrender 及模板、测试整条链路

## v0.4.9 (2026-08-31)

- 卡片接入 QQ 头像：个人 `q1.qlogo.cn`、群 `p.qlogo.cn` 公开 CDN，五张模板统一外链图，加载失败自动回退首字圆圈（非 QQ 平台 ID 天然回退）

## v0.4.8 (2026-08-31)

- 卡片渲染本地化：playwright + chromium 截图优先（模板与数据不出本机，实测 0.7~1.7 秒/张），未安装时回退云端 t2i，再回退文本，各级 45 秒超时
- `/聊天统计` 总览/趋势/时段接入图片卡片（逐日/逐时柱状图）；修复渲染服务过载返回 200+文本时框架不抛异常导致的假图片路径（增加图片魔数校验）
- 群报砍掉「关键词」分节、用户画像/群画像卡片移除关键词区块（词云即关键词的可视化）

## v0.4.7 (2026-08-18)

- HTML 卡片渲染上线：`render_mode=text|image`（默认 text），覆盖 `/用户画像`、`/群画像`、定时群报，渲染失败自动回退文本
- fix: `/用户画像 @某人` 报「无法识别的参数」——At 渲染文本混入参数未被解析（对齐词云的处理）

## v0.4.6 (2026-08-18)

- 群报时/分由下拉改为滑块（int + slider，更紧凑）；滑块值为 int 且 0 点合法，调度侧组合改用 int() 避免 `or` 回退吞掉 0 时

## v0.4.5 (2026-08-18)

- fix: 口语触发「@某人 词云」（At 在前语序）无法放行——首词判定现跳过 At 渲染文本（线上复测确认该形态静默无响应）

## v0.4.4 (2026-08-18)

- 群报配置全部改为约束输入（时/分/星期/频率下拉 + 每月日期滑块 1-31），调度循环移除已不可达的「配置无效重试」兜底分支，坏值仅可能来自手工改配置文件，由统一异常处理接管

## v0.4.3 (2026-08-18)

- 群报时间改为「时/分」两个下拉选择（原 `report_time` 自由文本废弃）——AstrBot 配置渲染器不支持输入校验，下拉从源头杜绝非法时间；升级后如配置异常请重新选择时间

## v0.4.1 (2026-08-18)

- fix: `/词云 @某人` 报「无法识别的参数」——At 段被平台渲染成 `@名字(QQ号)` 文本混入参数，解析器现识别该形态并提取 QQ 号（兼容全角括号），`/词云 @某人 [时间]`、`/发言榜 @某人` 恢复可用

## v0.4.0 (2026-08-18)

- 新功能：定时群报。频率三档 每日（报昨日）/ 每周（报上周）/ 每月（报上月，月末自动钳制）
- WebUI 完整设置：开关、频率/星期下拉、每月日期、时间、目标群、播报内容复选框、活跃度下限（静默群自动跳过）
- 配置改动约 5 分钟内生效，无需重启；分节独立容错

## v0.3.0 (2026-08-17)

- 破坏性变更：按真实使用数据砍除六项低价值统计——`/用户统计` 顶层命令、`/聊天统计` 的 emoji/类型/长度/转发/昼夜
- 画像命令重构：`/用户画像` 顶层命令一次输出六视图合并画像（综合/活跃/关键词/风格/互动/机器人）；`/昵称` 独立顶层命令；`状态/刷新` 移入 `/画像维护` 管理组
- 词云 PNG 自动清理（`wordcloud_retention_days`，默认 7 天）
- 测试收敛与补强（TTLCache 覆盖等）

## v0.2.0 (2026-08-17)

- 词云交互改造：默认查自己、`@某人` 查他人、「全群」显式触发；口语触发放行规则完善，唤醒前缀从 AstrBot 配置动态读取
- 命令中文化（用户画像子命令中文主名、发言榜为主名）
- 合并 astrbot_plugin_chat_statistics 与 astrbot_plugin_user_profile，测试体系建立

## v0.1.0 (2026-08-16)

- 首个版本：基于 ChatLogger 数据底座的只读统计插件——群聊统计、发言排行、词云关键词、用户画像、群画像
